[package]
name = "vinyl-compiler"
version = "0.1.0"
edition = "2024"

[dependencies]
vinyl-parser = { path = "../parser" }
vinyl-typecheck = { path = "../typecheck" }
miette = { version = "7", features = ["derive"] }
tracing = "0.1"
