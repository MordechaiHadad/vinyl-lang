[package]
name = "vinyl-parser"
version = "0.1.0"
edition = "2024"

[dependencies]
tree-sitter = "0.26.3"
miette = { version = "7", features = ["derive"] }
vinyl-diagnostics = { version = "0.1.0", path = "../diagnostics" }
thiserror = "2.0.19"

[build-dependencies]
cc = "1"
