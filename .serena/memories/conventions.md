Examples use `.vnl`, `#` comments, explicit `fn main()`, and trailing expressions for returns. Current supported data examples: tuple literals and numeric field access (`(1, 2).0`), enum variant construction with required call syntax (`Type::Variant()` / `Type::Variant(value)`), and equality. Struct and tuple declarations parse/typecheck, but struct literal construction is not implemented. Match expressions remain deferred.
