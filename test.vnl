# Example comment

fn average(a: int32, b: int32, c: int32, d: int32): int32 {
    # If the parser ignores the parentheses, it will divide 'a' 
    # instead of the sum.
    return (a + b + c + d) / 4;
}

fn main() {
    average(1, 2, 3, 4);
}
